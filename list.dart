/// Demonstrates the creation and printing of a list in Dart.
///
/// This example initializes a list of integers, `numeros`, which allows
/// duplicates and maintains order. The list is then printed to the console.
void main() {
  List<int> numeros = [1, 2, 3, 4, 2, 5];
  print(numeros);
}
